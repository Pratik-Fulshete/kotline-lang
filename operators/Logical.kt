//&& || ! 
fun main(){
    var age = 20
    var hasId = true
    println(age >= 18 && hasId) //true
    println(age <= 18 && hasId) //false
    println(age < 20 || hasId)//true
    println(age!=20 && hasId)//false
}